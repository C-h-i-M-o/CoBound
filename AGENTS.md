# AGENTS.md

## 项目规则

- 全程使用简体中文交流，包含说明、注释、错误提示和文档内容。
- 任何文件创建、修改、删除、安装依赖、修改配置、执行提交前，必须先获得用户明确授权。
- 优先复用现有工具、Skill、MCP和成熟开源方案；不要为了演示而手写复杂基础设施。
- 修改代码时同步更新相关文档，尤其是 `README.md`、本文件（即`AGENTS.md`）和 `docs/` 下的需求说明。
- 变更必须小而准，只触碰与当前任务直接相关的文件。
- 请保持本文件（即`AGENTS.md`）和`CLAUDE.md`文件内容完全相同，以便不同AI工具阅读

## 技术约定

- 首版开发规划：Next.js 全栈、TypeScript、SQLite + Prisma。
- Node.js 包管理优先使用 `pnpm`。
- Python 如需使用，默认解释器为 `/opt/anaconda3/bin/python`。
- TypeScript 必须使用完整类型定义，禁止使用 `any`；确需使用时必须用中文注释说明原因。
- 未来预留微信小程序接入能力，API设计应避免与单一Web客户端强绑定。

## 验证要求

- 文档变更后检查Markdown结构、链接、编号和术语一致性。
- 代码变更后至少执行类型检查、构建检查和与改动相关的测试。
- Git提交前检查 `git status --short`，避免提交无关文件。
